import React from 'react';
import './Loading.css';

function Loading() {
    return (
        <div className="Loading">
            <div className="Loading-Loader"/>
        </div>
    );
}

export default Loading;